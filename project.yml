# XcodeGen spec — generate the Xcode project with:  xcodegen generate
# (run from the ios/ directory). The .xcodeproj IS committed so CI and Xcode
# Cloud can build without running XcodeGen first — regenerate and commit it
# after editing this file.

name: AIUsageLimits

options:
  bundleIdPrefix: com.stavrop
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

settings:
  base:
    DEVELOPMENT_TEAM: NCZ7646BF7
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "1.0.1"
    CURRENT_PROJECT_VERSION: "5"
    CODE_SIGN_STYLE: Automatic

targets:
  AIUsageLimits:
    type: application
    platform: iOS
    sources:
      - App
      - Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.stavrop.ailimits
        # iPhone only. The layout targets one-handed phone use and has never been
        # tested on iPad; claiming iPad support would ship an unverified surface
        # (and require an App Store screenshot set for it).
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: App/Info.plist
        CODE_SIGN_ENTITLEMENTS: App/AIUsageLimits.entitlements
    dependencies:
      - target: UsageWidgetExtension
        embed: true

  UsageWidgetExtension:
    type: app-extension
    platform: iOS
    sources:
      - Widget
      - Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.stavrop.ailimits.widget
        TARGETED_DEVICE_FAMILY: "1"
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Widget/Info.plist
        INFOPLIST_KEY_CFBundleDisplayName: AI Usage Limits
        CODE_SIGN_ENTITLEMENTS: Widget/UsageWidget.entitlements

# A SHARED scheme (committed in xcshareddata) — required by Xcode Cloud.
schemes:
  AIUsageLimits:
    build:
      targets:
        AIUsageLimits: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
